import React from 'react'

interface headerProps {}

const Header: React.FC<headerProps> = () => {
    return (
        <div>
            header component
        </div>
    )
}

export default Header